[package]
edition = "2021"
name = "woodox-lib"
version = "0.1.0"

[dependencies]
defmt = "1"
usbd-human-interface-device = "0.6"
packed_struct = { version = "0", default-features = false }

[dev-dependencies]
embedded-hal-mock = "0.11"

[features]
default = ["macropad"]

macropad = []
